# render.yaml
services:
  - type: web
    name: employee-survey-api
    rootDir: api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        # paste your Neon URL (keep sslmode=require)
        value: postgresql+psycopg2://neondb_owner:npg_6A0yrdRMqSGc@ep-long-band-advthcgm-pooler.c-2.us-east-1.aws.neon.tech/appdb?sslmode=require
      - key: PYTHONUNBUFFERED
        value: "1"
